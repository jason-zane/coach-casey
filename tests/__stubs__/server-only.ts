// Test stub for Next.js' `server-only` package. The real package is a
// build-time marker that prevents server-only code from being bundled
// into the client. In tests we run on the server by definition; the
// marker is a no-op.
export {};
