import type { Metadata, Viewport } from "next";
import { Geist, IBM_Plex_Mono, Newsreader } from "next/font/google";
import "./globals.css";
import { ServiceWorkerRegistrar } from "./_components/service-worker-registrar";
import { APPLE_SPLASH_STARTUP_IMAGES } from "@/lib/pwa/splash-startup-images";
import { SITE_URL } from "@/lib/site-url";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const newsreader = Newsreader({
  variable: "--font-newsreader",
  subsets: ["latin"],
  weight: ["400", "500", "600"],
  style: ["normal", "italic"],
  display: "swap",
});

const plexMono = IBM_Plex_Mono({
  variable: "--font-plex-mono",
  subsets: ["latin"],
  weight: ["400", "500"],
  display: "swap",
});

export const metadata: Metadata = {
  metadataBase: new URL(SITE_URL),
  title: {
    default: "Coach Casey | AI running coach for marathoners on Strava",
    template: "%s | Coach Casey",
  },
  description:
    "Connect Strava, keep your marathon plan. Coach Casey reads every run, remembers your season, and writes back like a coach who's paying attention.",
  applicationName: "Coach Casey",
  openGraph: {
    type: "website",
    siteName: "Coach Casey",
  },
  manifest: "/manifest.json",
  appleWebApp: {
    capable: true,
    statusBarStyle: "default",
    title: "Coach Casey",
    // Universal apple-touch-startup-image: one light image scaled by iOS to
    // fit any device (the app is light-only, so no dark variant). The
    // previous per-device matrix needed updating every time Apple shipped a
    // new iPhone; this approach is future-proof and trims public/splash to a
    // single PNG. Generated by scripts/generate-splash.mts.
    startupImage: APPLE_SPLASH_STARTUP_IMAGES,
  },
  icons: {
    icon: [
      { url: "/favicon-32.png", sizes: "32x32", type: "image/png" },
      { url: "/favicon-16.png", sizes: "16x16", type: "image/png" },
    ],
    apple: "/apple-touch-icon.png",
  },
};

export const viewport: Viewport = {
  // Light-only app: a single paper theme-color for the browser/PWA chrome,
  // no prefers-color-scheme dark variant.
  themeColor: "#faf8f5",
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  viewportFit: "cover",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${newsreader.variable} ${plexMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-paper text-ink font-sans overflow-x-hidden">
        <ServiceWorkerRegistrar />
        {children}
      </body>
    </html>
  );
}
